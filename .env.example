# Google Apps Script Web App URL for the application/contact forms.
# Provided later by the daycare owner once the Apps Script backend (Phase 5)
# is deployed. Copy this file to .env.local and fill in the value locally;
# in Vercel, set it under Project Settings -> Environment Variables.
NEXT_PUBLIC_FORM_ENDPOINT=
