# /public/updates/

Drop update photos here (JPG/PNG/WebP). Reference them from
`src/data/updates.ts` as `/updates/your-file-name.jpg`.

This file exists just so the empty folder gets committed to git — it's
safe to leave in place alongside your photos.
