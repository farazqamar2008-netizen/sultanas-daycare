import { SectionPlaceholder } from "./SectionPlaceholder";

export function ServicesSection() {
  return (
    <SectionPlaceholder
      id="services"
      title="Services"
      blurb="Hours, meals, nap time, outdoor play, and everything else we offer will live here."
      phaseNote="Built in Phase 4"
      className="bg-sky-light"
    />
  );
}
