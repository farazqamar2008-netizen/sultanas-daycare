import { SectionPlaceholder } from "./SectionPlaceholder";

export function ApplySection() {
  return (
    <SectionPlaceholder
      id="apply"
      title="Apply Now"
      blurb="The enrollment application — parent info, child details, and schedule — will live here."
      phaseNote="Built in Phase 3"
      className="bg-grass-light"
    />
  );
}
